spring:
  flyway:
    enabled: true
  application:
    name: Person API
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres?currentSchema=telbook"
    username: "postgres"
    password: "1"
    driver-class-name: org.postgresql.Driver
server:
  servlet:
    context-path: /api
  port: 8081
